Fastify tap "hello world" example
=================================


## Up and running

- install yarn
- run `yarn`
- `yarn server` to run the server
- `yarn test` to run tests. Does not need server to be running
